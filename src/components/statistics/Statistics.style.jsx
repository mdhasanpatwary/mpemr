import { alpha, styled } from "@mui/material/styles";
import { Box } from "@mui/material";

export const StatisticsWrap = styled(Box)(({ theme }) => ({
  img: {
    borderRadius: ".625rem",
  },
}));
